import template from './country-selector.html';
import controller from './country-selector.controller';

let countrySelectorComponent = {
  template,
  controller,
  controllerAs: 'countrySelector'
};

export default countrySelectorComponent;
